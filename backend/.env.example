# SECURITY: Never commit the real .env file to version control.
# This is a template. Copy this to .env and fill in the values.

# ----------------------------------
# SECURITY SETTINGS
# ----------------------------------
# Set to False in production. CRITICAL.
DEBUG=False

# Generate a strong, unique secret key.
# You can generate one using Python: secrets.token_urlsafe(50)
SECRET_KEY=change-me-to-a-very-long-random-string

# Comma-separated list of domain names this site can serve.
# Example: myastrosite.com,www.myastrosite.com,12.34.56.78
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# ----------------------------------
# SSL & CORS SETTINGS
# ----------------------------------
# Trusted origins for CSRF (e.g., your domain with https://)
CSRF_TRUSTED_ORIGINS=https://your-domain.com

# Allowed origins for CORS (if you have a separate frontend, otherwise same as domain)
CORS_ALLOWED_ORIGINS=https://your-domain.com

# ----------------------------------
# APPLICATION SETTINGS
# ----------------------------------
# Set to True to make all premium features free (Dev/Demo mode)
# Set to False for production to enable paywalls.
FREE_PREMIUM_MODE=False

# Database settings are currently using SQLite by default.
# If you switch to PostgreSQL, add those credentials here.
